# Extract meaningful features from preprocessed data to be fed into classifiers. Ex: perform spectral analysis on data and build meaningful data structure out of it.

def append_spectral_features():
	return False

def append_amplitude features():
	return False

# Write processed, feature extracted data to file
def write_to_file(data, time_interval):
	# Return bool representing success/failure of file writing